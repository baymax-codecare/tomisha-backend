{{#> layout}}
  {{#*inline "content"}}
    <p class="mb-md">
      Hallo {{receiverName}},
      <br />
      <br />
      Du wurdest eingeladen von:
    </p>

    <div class="card mb-md">
      <div class="avatar">
        <img src="{{senderPicture}}" />
      </div>
      <div class="body">
        <div class="card-title">{{senderName}}</div>
        <div class="card-sub-title">{{senderProfession}}</div>
      </div>
    </div>

    <a href="{{href}}" rel="notrack" target="_blank" class="btn">
      Profil erstellen
    </a>
  {{/inline}}
{{/layout}}